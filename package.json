{
  "name": "reddit-reply-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that reads a Reddit post + comments and drafts tailored replies and DMs with Claude.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@google/generative-ai": "^0.24.1",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@types/chrome": "latest",
    "esbuild": "latest",
    "typescript": "latest"
  }
}
