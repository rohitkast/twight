# Changes to make today
- user can select if comments are needed to be sent. sometimes what is happening is i only require a DM for the poster. so comments are being sent without any use. so need to have some good UI in the sidepanel below the post details where user can check uncheck things to send. maybe have only one check. send comments too. below the post detail. even better. add this check box on the chat text box only. small at the top just like how we attach files on here and cursor chat window and the file name shows up in the text box at the top. we can have a small check box which when not selected gets dimmed and if selected gets highlighted. also change the functionality too. if comments are selected then only send it otherwise dont.